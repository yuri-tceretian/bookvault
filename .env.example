# Copy this file to `.env` and fill in your real litres.ru credentials.
# `.env` is gitignored -- it never gets committed.
#
# These credentials are used by the MCP server ONLY. The MCP server is
# headless (no login form), so it bootstraps a first session from these on
# first run. The web app does NOT use them: you log in through its login
# page, and the session is saved (cookies + OS keychain) and reused from
# then on. So if you only use the web app, you can leave these unset.

LITRES_LOGIN=you@example.com
LITRES_PASSWORD=your-litres-password

# Everything below is optional -- sensible defaults are used if unset.

# Web UI port.
# LITRES_APP_PORT=8420

# Bind host for the web server. Leave as 127.0.0.1 for a normal local run --
# this app is localhost-only by design. The Docker image sets 0.0.0.0 and
# publishes to 127.0.0.1 on the host (see README "Running in Docker"). Do not
# set 0.0.0.0 outside a container.
# LITRES_APP_HOST=127.0.0.1

# Auto-reload the web server on code changes (handy in dev). The Docker image
# sets this to 0.
# LITRES_RELOAD=1

# MCP transport: "stdio" (default -- the client launches the process) or
# "streamable-http" (a networked service, used by the Docker image). With
# streamable-http the server binds LITRES_MCP_HOST:LITRES_MCP_PORT and clients
# connect to http://host:port/mcp.
# LITRES_MCP_TRANSPORT=stdio
# LITRES_MCP_HOST=127.0.0.1
# LITRES_MCP_PORT=8421

# Where the MCP server's download_book tool saves files (flat layout) when
# LITRES_LIBRARY_DIR is unset.
# LITRES_DOWNLOAD_DIR=/Users/you/Downloads/litres-library

# On-disk library root in litres-downloader / Audiobookshelf layout:
#   {LITRES_LIBRARY_DIR}/{Author}/{Title}/metadata.json + media files
# When set, web "Sync library", MCP download_book/sync_library_now, and
# optional autosync write here. Leave unset to disable library writes.
# LITRES_LIBRARY_DIR=/path/to/audiobooks

# Periodic background sync into LITRES_LIBRARY_DIR (web app). Requires the
# library dir above. Off by default.
# LITRES_AUTOSYNC=0
# LITRES_AUTOSYNC_INTERVAL=600
# LITRES_AUTOSYNC_AUDIO_ONLY=1
# LITRES_AUTOSYNC_ON_START=1

# Where the browser session (cookies) is cached between runs.
# LITRES_SESSION_FILE=/absolute/path/to/.litres_session.json

# Per-file download timeout in milliseconds. Whole-audiobook bundles can be
# ~2GB; raise this on a slow connection, lower it to fail faster on a stuck
# transfer.
# LITRES_DOWNLOAD_TIMEOUT_MS=300000

# Set to 0 to watch the login flow in a real (non-headless) Chromium
# window -- useful for debugging a login/selector problem.
# LITRES_HEADLESS=1

# Logging verbosity for the app's own log lines (login, library listing,
# download progress, ...). One of DEBUG/INFO/WARNING/ERROR.
# LITRES_LOG_LEVEL=INFO

# Where the library listing and per-book file-listing cache is stored (see
# app/cache.py) -- this is what lets a page reload or app restart skip
# re-querying litres.ru for data it already has.
# LITRES_CACHE_FILE=/absolute/path/to/.litres_cache.json

# Where the shared UI state (which books are selected + the preferred ebook/
# audiobook formats) is stored. This lives on the server so every browser shows
# the same selection, formats, and progress; persisting it here also survives a
# restart. In Docker it defaults onto the /data volume.
# LITRES_STATE_FILE=/absolute/path/to/.litres_state.json

# How long (seconds) the cached library listing is considered fresh before
# it's re-fetched. Buying something new won't show up until this expires,
# or you click the UI's Refresh button.
# LITRES_LIBRARY_CACHE_TTL=900

# How long (seconds) a book's cached file listing (used for size display
# and downloads) is considered fresh. Long-lived by default -- a purchased
# book's available formats essentially never change.
# LITRES_FILES_CACHE_TTL=604800

# Anti-bot (DDoS-Guard) resilience. On a transient block (DDoS-Guard 403 /
# 429 / 503) the client backs off, re-warms its __ddg* cookies, and retries
# rather than failing the item and moving on. Tune how hard it tries:
#   LITRES_MAX_RETRIES       how many times to retry a blocked request (default 3)
#   LITRES_RETRY_BASE_DELAY  first backoff in seconds, doubled each retry (default 2.0)
#   LITRES_RETRY_MAX_DELAY   cap on any single backoff, seconds (default 30.0)
# LITRES_MAX_RETRIES=3
# LITRES_RETRY_BASE_DELAY=2.0
# LITRES_RETRY_MAX_DELAY=30.0

# Base gap (seconds) between *live* per-book size fetches during a sweep;
# the real gap is jittered up from this. Cache hits skip it entirely.
# LITRES_SIZE_CHECK_PACE=0.2
